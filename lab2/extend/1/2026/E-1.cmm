struct A { int x; int y; };
struct B { int u; float v; };

int foo(int a);

int bar(struct A p){
    return p.x + p.y;
}

int main() {
    struct A a;
    struct B b;
    a.x = 1;
    a.y = 2;
    b.u = 3;
    b.v = 4.0;
    bar(a);
    return 0;
}

int bar(struct B q);
