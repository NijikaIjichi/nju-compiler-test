struct Pet {
  int age;
  float weight;
};

int is_older(struct Pet p1, struct Pet p2) { return p1.age > p2.age; }

int main() {
  struct Pet a, b, c;

  a.age = 5;
  a.weight = 12.5;
  b.age = 10;
  b.weight = 0.6;
  c.age = 7 - 6;
  c.weight = 3.5;

  is_older(a, b, c);

  return 0;
}